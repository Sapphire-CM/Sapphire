<%= f.simple_fields_for :exercise_registrations, ExerciseRegistration.new(submission: submission), child_index: "new_item" do |exercise_registration| %>
  <%= render "exercise_registration_fields", f: exercise_registration %> %>
<% end %>