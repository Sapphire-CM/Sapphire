var $comment = $("#<%= dom_id(@comment) %>");
$comment.remove()

if ($(".comments_list").find('.comment').length == 0) {
  var html = "<%=j render "comments/no_comments" %>";
  $(".comments_list").html(html);
}
