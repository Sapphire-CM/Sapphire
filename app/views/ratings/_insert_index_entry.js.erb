var html = "<%= escape_javascript render partial: 'index_entry', locals: { rating: @rating } %>";

$rating_group = $("<%= "#rating_group_id_#{rating.rating_group.id}" %>")
$rating_group.find(".rating_index_entries").children(".last_one").before(html)
$rating_group.find(".index_entry_none").hide();

$('#reveal_modal').foundation('reveal', 'close');