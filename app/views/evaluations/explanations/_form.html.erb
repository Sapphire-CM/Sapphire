<%= simple_form_for(@comment, url: evaluation_explanations_path(@commentable), remote: true, html: { id: dom_id(@comment, 'form') }) do |f| %>
  <%= render "form_fields", f: f, type:'explanation' %>
<% end %>
