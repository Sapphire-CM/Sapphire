<% event_type_service = EventTypeService.new %>

<%= form_tag term_events_path(term) do %>
  <% event_type_service.types.keys.each do |type_id| %>
    <label>
      <%= check_box_tag(:scopes, type_id) %>

      <%= type_id %>
    </label>
  <% end %>
<% end %>