var formContent = "<%=j render 'edit_form', comment: @comment, type: @comment.name %>"
var $form = $('#<%= dom_id(@comment, 'edit') %>')

if ($form.length > 0) {
  $form.replaceWith(formContent);
} else {
  $('#<%= dom_id(@comment) %>').replaceWith(formContent);
}
