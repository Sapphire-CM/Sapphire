<%= event_box(created) do |event| %>
  <div class='title'>
    <%= event.account.fullname %> <strong>wrote</strong> <%= indefinite_articlerize(event.name) %> <strong>comment</strong> for <%= link_to event.commentable_type, polymorphic_path(event.index_path[:klass], id: event.index_path[:id]) %>.
  </div>
<% end %>
