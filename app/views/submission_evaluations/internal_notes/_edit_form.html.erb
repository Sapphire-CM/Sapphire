<%= simple_form_for(@comment, url: submission_evaluation_internal_note_path(@commentable, @comment), method: :patch, remote: true) do |f| %>
  <%= render "edit_form_fields", f: f, cancel_path: submission_evaluation_internal_note_path(@commentable, @comment) %>
<% end %>
