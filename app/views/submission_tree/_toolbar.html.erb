<div>
  <%= link_to foundation_icon(:upload) + ' Upload', new_submission_upload_path(submission), class: "tiny button", data: { reveal_id: "upload-modal" } %>
  <%= link_to foundation_icon(:archive) + ' Download', blob_submission_path(submission, path: params[:path], format: :zip), class: "tiny button#{" disabled" if tree.entries.empty?}" %>
  <%= link_to foundation_icon(:folder_add) + ' Folder', '#', class: "tiny button", data: { reveal_id: "change-folder-modal" } %>
  <%#= link_to foundation_icon(:page_filled) + ' Move', '#', class: "tiny disabled button" %>
</div>
