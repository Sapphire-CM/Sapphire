<%= simple_form_for @course do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  
  <%= link_to "Cancel", (@course.new_record? ? courses_path : course_path(@course)), :class => "secondary button" %>
  <%= f.submit :class => "button" %>
<% end %>