<%= event_box(updated) do |event| %>
  <div class='title'>
    <%= event.account.fullname %> <strong>edited</strong> <%= event.name %> <strong>comment</strong> for <%= link_to event.commentable_type, polymorphic_path(event.commentable_index.first, id: event.commentable_index.second) %>.
<% end %>
