<%= simple_form_for(@comment, url: submission_evaluation_feedback_path(@commentable, @comment), method: :patch, remote: true) do |f| %>
  <%= render "edit_form_fields", f: f, cancel_path: submission_evaluation_note_path(@commentable, @comment) %>
<% end %>
